<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /stagging/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /stagging/index.html [L]
</IfModule>
