# MongoDB Configuration
# Option 1: provide the full URI
MONGODB_URI=mongodb+srv://photograper:photograper@cluster0.sy94kcl.mongodb.net/?appName=Cluster0

# Option 2: let the server build the URI from the pieces below
MONGODB_USERNAME=photograper
MONGODB_PASSWORD=photograper
MONGODB_HOST=cluster0.ds2nlug.mongodb.net
MONGODB_APP_NAME=Cluster0

DATABASE_NAME=photograper

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
